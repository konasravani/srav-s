<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title> vs Bulb On / Off (HTML + CSS)</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    :root{
      --off-color: #cfcfcf;
      --on-color: #ffd54f;
      --bg-off: #f6f7fb;
      --bg-on: #fff9e6;
    }

    html,body{height:100%;margin:0;font-family:system-ui,Segoe UI,Roboto,Arial;background:var(--bg-off);display:flex;align-items:center;justify-content:center}
    .card{width:320px;padding:20px;border-radius:12px;box-shadow:0 8px 24px rgba(0,0,0,0.08);text-align:center;background:white}

    /* hide the checkbox itself */
    #toggle{position:absolute;left:-9999px}

    /* bulb container */
    .bulb{
      width:140px;
      height:180px;
      margin: 6px auto 12px;
      transition: all .25s ease;
      filter: drop-shadow(0 0 0 rgba(0,0,0,0));
    }

    /* default (off) styles applied to SVG shapes */
    .bulb .lamp-fill{fill:var(--off-color)}
    .bulb .glow{opacity:0;transition:opacity .25s ease}

    /* button styling */
    .toggle-btn{
      display:inline-block;
      margin-top:12px;
      padding:10px 16px;
      border-radius:8px;
      border:1px solid #e6e6e6;
      background:#fafafa;
      cursor:pointer;
      user-select:none;
      font-weight:600;
    }

    /* when checkbox is checked -> bulb on */
    #toggle:checked ~ .card { background: linear-gradient(180deg, var(--bg-on), #fff); }
    #toggle:checked ~ .card .bulb .lamp-fill { fill: var(--on-color); }
    #toggle:checked ~ .card .bulb { filter: drop-shadow(0 6px 24px rgba(255,197,79,0.25)); }
    #toggle:checked ~ .card .bulb .glow { opacity: 1; }
    #toggle:checked ~ .card .toggle-btn { background: #ffd54f; border-color:#ffcc33; color:#333; }

    /* small helper text */
    .label{color:#444;margin:8px 0 4px}
    .hint{color:#777;font-size:13px;margin-top:8px}
  </style>
</head>
<body>

  <!-- hidden checkbox toggles the UI using CSS sibling selectors -->
  <input id="toggle" type="checkbox" />

  <div class="card" role="region" aria-label="Light bulb toggle">
    <div class="label">Bulb — Click the button to toggle</div>

    <!-- Bulb SVG: lamp-fill is the main fill; glow is a radial ellipse for glow effect -->
    <svg class="bulb" viewBox="0 0 80 100" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
      <!-- glow behind the bulb -->
      <ellipse class="glow" cx="40" cy="40" rx="30" ry="18" fill="#fff3b0" opacity="0.0"/>
      <!-- bulb glass -->
      <path class="lamp-fill" d="M40 6c-10 0-18 8-18 18 0 8 5 14 7 17 1.6 2 3 4 3 6v9c0 2.8-2.2 5-5 5H30c-1.1 0-2 .9-2 2v6c0 1.1.9 2 2 2h20c1.1 0 2-.9 2-2v-6c0-1.1-.9-2-2-2h-5c-2.8 0-5-2.2-5-5V47c0-2 1.4-4 3-6 2-3 7-9 7-17 0-10-8-18-18-18z"/>
      <!-- screw base -->
      <rect x="28" y="66" width="24" height="10" rx="3" fill="#b0b0b0"/>
      <rect x="31" y="76" width="18" height="6" rx="2" fill="#8f8f8f"/>
      <!-- filament hint (for look) -->
      <path d="M34 36c1.5-1 3.5-1 5 0 1.5 1 3.5 1 5 0" fill="none" stroke="#8a5a2b" stroke-width="1.5" stroke-linecap="round"/>
    </svg>

    <!-- label is clickable because it's for the hidden checkbox -->
    <label class="toggle-btn" for="toggle" role="button" aria-pressed="false">Toggle Bulb</label>

    <div class="hint">No JavaScript — purely HTML + CSS</div>
  </div>

</body>
</html>

